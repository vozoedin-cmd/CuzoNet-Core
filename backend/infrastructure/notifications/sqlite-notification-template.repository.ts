/**
 * Notification templates are intentionally code-backed in this version.
 * Keeping this module documents that no persisted template repository is required.
 */
export const notificationTemplatesAreCodeBacked = true;
